import type { Migration } from '@cossackframework/orm';

export default {
  name: '{{name}}',
  async up({ orm, schema }) {
    // Queue schema operations here. For custom SQL:
    // schema.raw(orm.sql.fragment`CREATE TABLE ...`);
  },
  async down({ orm, schema }) {
    // Reverse the operations above.
  },
} satisfies Migration;
