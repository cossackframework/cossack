import { defineSeeder } from '@cossackframework/orm';

export default defineSeeder({
  name: '{{name}}',
  transaction: 'auto',
  async run({ orm, sql }) {
    // Seed through Active Record models, Entity.query(), or bound `sql`.
  },
});
